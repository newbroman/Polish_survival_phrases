import { defineConfig } from 'vite'

export default defineConfig({
    base: '/Polish_survival_phrases/',
    build: {
        rollupOptions: {
            output: {
                entryFileNames: `assets/[name].js`,
                chunkFileNames: `assets/[name].js`,
                assetFileNames: `assets/[name].[ext]`
            }
        }
    }
})
